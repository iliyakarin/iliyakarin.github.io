---
interface Props {
    name: string;
    class?: string;
}

const { name, class: className } = Astro.props;
---

<svg class={className} fill="currentColor">
    <use href={`#icon-${name}`} />
</svg>
